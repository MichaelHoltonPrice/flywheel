name: HelloAgent
image: flywheel-claude:latest
docker_args:
  - -v
  - claude-auth:/home/claude/.claude:rw
env:
  MAX_TURNS: "1"
state: managed
inputs:
  - name: prompt
    container_path: /prompt
outputs:
  normal: []
