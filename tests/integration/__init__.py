"""Integration tests requiring live external dependencies.

Tests in this package are marked ``@pytest.mark.live_api`` and are
skipped by default.  Opt in with ``--run-live-api``; see
``tests/integration/README.md`` for setup and invocation.
"""
